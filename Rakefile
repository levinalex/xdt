# -*- ruby -*-

require 'rubygems'
require 'hoe'
require './lib/xdt.rb'

Hoe.new('xdt', Xdt::VERSION) do |p|
  # p.rubyforge_name = 'xdtx' # if different than lowercase project name
  # p.developer('FIX', 'FIX@example.com')
end

# vim: syntax=Ruby
